# Weekly schedule (template)

Per-user locked rhythm belongs in `user_program_memory.weekly_schedule` (Supabase).
Magnus loads it per `user_profile_id` — not from this shared file.

## Targets

| Lane | Frequency | Timing |
| ---- | --------- | ------ |
| **Training** | | |
| **Recovery** | | |
