# Health — user context (per-user)

This file is a **template**. Owner-specific program memory lives in Supabase
`user_program_memory` for each `user_profile_id`. Seed via `scripts/provision-owner-user.mts`.

## Goals

- (Set during health onboarding or through conversation with Magnus.)

## Training program

- (Your split, timing preferences, and equipment.)

## Integrations

- Hevy, if connected, is stored per user in `user_integrations`.
