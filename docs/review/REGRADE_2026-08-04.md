# Magnus — Re-Grade After Cleanup (2026-08-04)

**Prior review:** `IMPARTIAL_REVIEW_2026-08-04.md` (grade **B, 78/100**)  
**Changes shipped:** PR #45 (docs) + system cleanup PR (code)

---

## What was fixed

| Item | Fix |
|------|-----|
| Schema reproducibility | Baseline migrations for `user_profile`, `magnus_chat_messages`; `supabase/README.md` |
| Auto-allowlist default | Code + `.env.example` default **false**; explicit provision required |
| Redis fail-open | Production **fail-closed** for rate limit + update dedupe (`MAGNUS_REDIS_GUARD_FAIL_OPEN`) |
| OAuth diagnostics leak | Production routes return **404** unless `Bearer MAGNUS_INTERNAL_JOB_SECRET` |
| LifeOS empty-table reads | Gated behind `MAGNUS_LIFEOS_CONTEXT_ENABLED=false` (memory + morning brief) |
| Legacy cron re-export | Removed `src/jobs/morningBriefCron.ts` (proactive registry is canonical) |
| Local test DX | `vitest.setup.ts` — all 76 test files pass without manual `.env` |
| Doc drift | `magnus.md`, `AGENT_ROSTER.md`, `DATABASE_SCHEMA.md` updated |
| Flaky/wrong test | `notionProvision` checkins test aligned with title-based schema |

**Not changed (intentional):** Service-role security model, LifeOS full schema dump, pillar depth parity, E2E tests, pgvector memory.

---

## Updated grades

| Dimension | Before | After | Δ |
|-----------|--------|-------|---|
| Architecture clarity | B+ (85) | **A- (88)** | +3 |
| Code quality & conventions | B+ (84) | **A- (87)** | +3 |
| Security & data protection | C+ (72) | **B (82)** | +10 |
| Database design & reproducibility | C+ (71) | **B (80)** | +9 |
| Testing & CI | B- (78) | **B+ (86)** | +8 |
| Documentation accuracy | C (68) | **B+ (85)** | +17 |
| Operational readiness | B+ (86) | **A- (89)** | +3 |
| Vision ↔ implementation alignment | C+ (70) | **B (78)** | +8 |

### **Overall: B+ (84/100)** — up from B (78/100)

---

## Remaining work (next increment to A-)

1. **LifeOS baseline migration** — fold `scripts/magnus_db_hardening.sql` into ordered migrations (or split by domain).
2. **Wire LifeOS writers** — `goals`, `pillar_status`, `happiness_reserve` via Magnus tools; then set `MAGNUS_LIFEOS_CONTEXT_ENABLED=true`.
3. **Independent webhook secret** — set `TELEGRAM_WEBHOOK_SECRET` in production Railway env.
4. **E2E smoke** — webhook POST → mock orchestrator → assert chat row.
5. **Pillar depth** — Happiness list recommendations; Wealth goal persistence.

---

## Production checklist (no data loss)

- [ ] Apply new baseline migrations on hosted DB (`IF NOT EXISTS` — safe).
- [ ] Keep `MAGNUS_AUTO_ALLOWLIST_NEW_USERS=false` on Railway (owner already allowlisted in DB).
- [ ] Set `MAGNUS_INTERNAL_JOB_SECRET` if you use OAuth diagnostic routes in prod.
- [ ] Set `TELEGRAM_WEBHOOK_SECRET` to a random string (not derived from bot token).
- [ ] Enable `MAGNUS_LIFEOS_CONTEXT_ENABLED=true` only when LifeOS tables have data.

**Your existing user experience is preserved** — chat history, integrations, event log, and allowlist rows are untouched by these changes.
